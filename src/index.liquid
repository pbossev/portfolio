---
title: Peter Bossev 👋
layout: index.liquid
---

<div class="space-y-2">
  <div class="row mt-8 mb-4">
    <h1>Peter Bossev
      <span class="wave">👋</span>
    </h1>
    <h1>
      <a href="mailto:peterbossev@gmail.com" class="green arrow-external">Email</a>
    </h1>
  </div>
  <p>A highschool student with a passion for
    <strong>tech</strong>
    and
    <strong>math</strong>. Read more
    <a href="/about/">about me</a>, check out my
    <a href="https://pbsv.photo/" class="arrow-external">photography</a>, or get in contact through one of my
    <a href="/links/">links</a>.
  </p>
</div>

<div>
  <h2 class="mb-2">Projects 🛠️</h2>
  <div class="space-y-2">
    {% assign projects = collections.projects | sort: "data.order" %}
    {%- for project in projects -%}
      <div>
        <div>
          <a href="{{ project.url }}">{{ project.data.title }}</a>
          {% if project.data.stars %}
            <small>{{ project.data.stars }}★</small>
          {% endif %}
        </div>
        <p class="text-zinc-400 ">{{ project.data.description }}</p>
        {% if project.data.links.size > 0 %}
          <small>
            [
            {% for link in project.data.links %}
              <a href="{{ link.url }}">{{ link.label }}</a>
              {% if forloop.last == false %}
                |
              {% endif %}
            {% endfor %}]
          </small>
        {% endif %}
      </div>
    {%- endfor -%}
  </div>
</div>

<div>
  <h2 class="mb-2">Notes 📝</h2>
  <div class="space-y-2">
    {% assign notes = collections.notes | sort: "date" | reverse %}
    {%- for note in notes -%}
      <div class="row">
        <a href="{{ note.url }}">{{ note.data.title }}</a>
        <div class="grow border-zinc-700 border-b"></div>
        <p class="text-zinc-500">{{ note.data.dateLabel }}</p>
      </div>
    {%- endfor -%}
  </div>
</div>
<div>
  <h2 class="mb-2">Extracurriculars 🎓</h2>
  <div class="space-y-2">
    {%- for extracurricular in collections.extracurriculars -%}
      <div>
        <div class="row">
          <a href="{{ extracurricular.url }}">{{ extracurricular.data.title }}</a>
          <hr class="border-zinc-100">
          <small class="text-zinc-500">{{ extracurricular.data.dateLabel }}</small>
        </div>
        <p class="text-zinc-400">{{ extracurricular.data.description }}</p>
      </div>
    {%- endfor -%}
  </div>
</div>